# Raw data

Place unmodified Incucyte `.txt` exports in this directory. The pipeline reads
the metadata block inside each export and does not require a particular filename
unless filename-derived design fields are wanted.

Raw exports are ignored by Git to avoid publishing research data accidentally.
Keep this README file in the repository.
