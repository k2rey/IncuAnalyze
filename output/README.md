# Output

`analysis/run_pipeline.R` writes generated tables plus PDF and PNG overview
plots here. The optional interpolation table appears when interpolation is
enabled. Generated files are ignored by Git; this README keeps the directory
structure.
